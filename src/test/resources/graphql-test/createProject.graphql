mutation createProject(
    $projectCreateRequest: ProjectCreateRequest
) {
    createProject(projectCreateRequest: $projectCreateRequest) {
        id
        title
        bio
        content
    }
}
