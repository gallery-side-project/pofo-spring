mutation updateProject(
    $projectUpdateRequest: ProjectUpdateRequest
) {
    updateProject(
        projectUpdateRequest: $projectUpdateRequest
    ) {
        id
        title
        bio
        content
    }
}
